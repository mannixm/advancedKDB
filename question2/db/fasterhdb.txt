https://code.kx.com/q4m3/14_Introduction_to_Kdb+/#144-segmented-tables
https://code.kx.com/q/database/segment/
https://code.kx.com/q/wp/multi-thread/

Multi-threaded HDB start additional slaves with on command line with "-s", use peach or .Q.fc to distribute the work
Segmented HDB, a query can make use of multiple I/O channels and associated bandwidth and hence pull data concurrently. Segmentation spreads a partitioned table’s records across multiple directories, the segment directories can be placed on independent I/O channels so that data retrieval can occur in parallel.
